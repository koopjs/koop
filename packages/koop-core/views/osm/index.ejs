
<div id="map"></div>
  <div class="load-form">
    <input type="text" id="where" class="form-control pull-left" placeholder="Enter a filter" style="width:300px">
    <button class="btn btn-primary pull-right" onClick="load()">ReLoad</button>
  </div>

<script>

  function load(){
    var val = document.getElementById('where').value;
    if ( val ) {
      location.href = location.pathname + '?where=' + val ;
    }
  }

  $(function(){
    var mapDom = 'map';
    $('#'+mapDom).css({ width:document.width, height: document.height });
    koop = koopMap( mapDom );
    where = "<%= where %>";
    //if (document.getElementById('where').value !== ''){
    //  
    //}
    setTimeout(function(){
      alert('add');
      console.log(koop, location.origin + '/osm/points/FeatureServer/0');
      koop.add(location.origin + 'osm/points/FeatureServer/0');
    },1000);
  });

</script>

