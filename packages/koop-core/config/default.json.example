// Default config
{
    "server": {
        "port": 1337
    },

    // the number of children to start - defaults to the number of CPUs 
    processes: 1,

    //the local dir for storing tile files
    data_dir: '/usr/local/koop/',

    // if a logfile is defined all logs will go to it, otherwise console used for logging
    "logfile": "/var/log/koop.log",

    "db": {
        "postgis": {
            "conn": "postgres://localhost/koopdev"
        }
    }

    // Spatialite configuration (would replace PostGIS)
    //"db": {
    //    "sqlite": {
    //        "filename": ":memory:"
    //    }
    //}
}
